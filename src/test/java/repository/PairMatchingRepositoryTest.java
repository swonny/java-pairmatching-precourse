package repository;

import org.junit.jupiter.api.Test;

class PairMatchingRepositoryTest {
    @Test
    public void testPairRepository() {
//        Pair pair = new Pair(Arrays.asList(new Crew(Course.FRONTEND, "승원")));
//        Pair pair2 = new Pair(Arrays.asList(new Crew(Course.FRONTEND, "승원")));
//        PairMatchingRepository.add(
//                Course.FRONTEND,
//                Mission.CAR_RACING, Arrays.asList(
//                        pair
//                )
//        );
//        System.out.println(PairMatchingRepository.hasUniquePairsOnly(Course.FRONTEND, Mission.CAR_RACING, Arrays.asList(pair, pair2)));
    }
}